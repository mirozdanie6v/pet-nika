'use client';
import { createContext, useContext, useMemo, useSyncExternalStore } from 'react';
import { getServerSnapshot, getSnapshot, resetState, subscribe, updateState } from '@/lib/store';
import type { AppState, AppointmentRequest, Language, Pet, Reminder, RequestStatus } from '@/types';
interface AppContextValue { state: AppState; setLanguage: (language: Language) => void; setActivePet: (id: string) => void; savePet: (pet: Pet) => void; addPet: (pet: Pet) => void; addRequest: (request: AppointmentRequest) => void; setRequestStatus: (id: string, status: RequestStatus) => void; sendReminder: (id: string) => void; resetDemo: () => void; }
const AppContext = createContext<AppContextValue | null>(null);
export function AppProvider({ children }: { children: React.ReactNode }) { const state = useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot); const value = useMemo<AppContextValue>(() => ({ state, setLanguage: (language) => updateState((s) => ({ ...s, language })), setActivePet: (activePetId) => updateState((s) => ({ ...s, activePetId })), savePet: (pet) => updateState((s) => ({ ...s, pets: s.pets.map((p) => p.id === pet.id ? pet : p) })), addPet: (pet) => updateState((s) => ({ ...s, pets: [...s.pets, pet], activePetId: pet.id })), addRequest: (request) => updateState((s) => ({ ...s, requests: [request, ...s.requests] })), setRequestStatus: (id, status) => updateState((s) => ({ ...s, requests: s.requests.map((r) => r.id === id ? { ...r, status } : r) })), sendReminder: (id) => updateState((s) => ({ ...s, reminders: s.reminders.map((r: Reminder) => r.id === id ? { ...r, sent: true } : r) })), resetDemo: resetState, }), [state]); return <AppContext.Provider value={value}>{children}</AppContext.Provider>; }
export function useApp() { const ctx = useContext(AppContext); if (!ctx) throw new Error('useApp must be used inside AppProvider'); return ctx; }
